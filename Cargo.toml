[package]
name = "sourcery"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5.50", features = ["derive"] }
config = "0.15.18"
serde = { version = "1.0.228", features = ["derive"] }
toml = "0.9.8"

use crate::config::Config;
use crate::system::SystemInfo;

pub fn handle_clean(_config: &Config, _system_info: &SystemInfo) {
    println!("Cleaning old sources and outputs...");
    // TODO: Implement cleanup logic
}
